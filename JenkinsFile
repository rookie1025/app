node('jenkins-slave') {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
            rm -rf app
            git clone https://github.com/rookie1025/app.git
            docker build . -t test
        """)
    }
}
