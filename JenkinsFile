node() {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
            rm -rf app
            git clone https://github.com/rookie1025/app.git
            sudo yum install docker -y 
            sudo service docker start
            docker build . -t test
        """)
    }
}
